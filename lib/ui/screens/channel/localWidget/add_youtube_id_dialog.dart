import '../../../../utils/index.dart';

class AddYoutubeIdDialog extends StatelessWidget {
  const AddYoutubeIdDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      child: Container(
        child: Column(
          children: [Text("aim")],
        ),
      ),
    );
  }
}
